version: 2.1
jobs:
  destroy:
    docker:
      - image: hashicorp/terraform
    steps:
      - attach_workspace:
          at: .
      - run: terraform destroy -auto-approve 

workflows:
  destroy-infra:
    jobs:
      - destroy
